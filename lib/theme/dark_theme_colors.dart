import 'package:flutter/material.dart';

/// Colors used to define the app's UI if the system is in `dark mode`
class DarkThemeColors {
  DarkThemeColors._();

  static const primary = Color(0x00000005);
  static const secondary = Color(0x00000004);
  static const danger = Color(0x02ead9e9);

  static const onPrimary = Color(0x00001fbb);
  static const onSecondary = Color(0x0000b54f);
}
