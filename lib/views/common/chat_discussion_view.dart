import 'package:flutter/material.dart';

/// View displaying discussion between a `Student` and an `Employer`
class ChatDiscussionView extends StatelessWidget {
  const ChatDiscussionView({super.key});

  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: Scaffold(
        appBar: AppBar(),
      ),
    );
  }
}
